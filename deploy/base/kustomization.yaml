apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: platform-lab

resources:
  - namespace.yaml
  - service-account.yaml
  - deployment.yaml
  - service.yaml

labels:
  - pairs:
      app.kubernetes.io/name: demo-service
      app.kubernetes.io/part-of: gitops-platform-lab
    includeSelectors: true
